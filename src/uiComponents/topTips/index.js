import Vue from 'vue'const TopTips = Vue.extend(require('./index.vue'));//挂载const topTipsInstance = new TopTips({    el:document.createElement('div')});const show = (options={}) =>{    topTipsInstance.text = options.text;    topTipsInstance.icon = options.icon;    document.body.appendChild(topTipsInstance.$el);};const hide = () =>{    let elem =topTipsInstance.$el;    elem.parentNode && elem.parentNode.removeChild(elem);};export default {    show:show,    hide:hide};