import Vue from 'vue'const Tip = Vue.extend(require('./src/index.vue'));const instance = new Tip({   el:document.createElement('div')});const show = (options={})=>{    let elem = instance.$el;    elem&&document.body.appendChild(elem);};export default {    show:show}