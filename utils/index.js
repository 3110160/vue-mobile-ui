"use strict";const isIos = ()=>{    let u = navigator.userAgent;    return !!u.match(/\(i[^;]+;( U;)? CPU.+Mac OS X/); //ios终端};/*** @param el* @return parentNode * */const getScrollView = (el)=>{    let currentNode = el;    while (currentNode && currentNode.tagName !== 'HTML' && currentNode.tagName !== 'BODY' && currentNode.nodeType === 1) {        let overflowY = document.defaultView.getComputedStyle(currentNode).overflowY;        if (overflowY === 'scroll' || overflowY === 'auto') {            return currentNode;        }        currentNode = currentNode.parentNode;    }    return window;};const hasClass = (el,hasClass)=>{    return !!el.className&&el.className.match( new RegExp( "(\\s|^)" + hasClass + "(\\s|$)") );};const addClass = (el,addClass)=>{    if(hasClass(el,addClass)){        return false;    }    el.className = el.className+' '+addClass;};const removeClass = (obj, cls)=>{    if (hasClass(obj, cls)) {        let reg = new RegExp('(\\s|^)' + cls + '(\\s|$)');        obj.className = obj.className.replace(reg, ' ');    }};export {isIos,getScrollView,addClass,removeClass}